/**
 * Adresse du serveur backend (celui du dossier `backend/` de ce projet).
 *
 * IMPORTANT : sur un téléphone physique, "localhost" désigne le téléphone
 * lui-même, pas votre ordinateur. Remplacez par l'adresse IP locale de la
 * machine qui exécute `npm start` dans backend/ (ex: 192.168.1.42).
 *
 * Pour trouver cette adresse :
 *   - Windows : ouvrez PowerShell, tapez `ipconfig`, cherchez "Adresse IPv4"
 *   - macOS/Linux : tapez `ifconfig` ou `ip addr`
 *
 * Le téléphone et l'ordinateur doivent être sur le même réseau Wi-Fi.
 */
export const API_BASE_URL = "http://192.168.0.54:3001";
